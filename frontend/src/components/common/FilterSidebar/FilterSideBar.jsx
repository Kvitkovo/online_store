import React from 'react';
import styles from './FilterSidebar.module.scss';

const FilterSidebar = () => {
  return <div className={styles.filterContainer}>Sidebar</div>;
};

export default FilterSidebar;
